# Idea Tree (Jekyll site)

This repository contains the Idea Tree Jekyll site (GitHub Pages compatible).

To run locally:

```
bundle install
bundle exec jekyll serve
```

Then open http://127.0.0.1:4000/ideatree/
